[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[variables]
NODE_VERSION = "20"
NPM_CONFIG_PRODUCTION = "true"
NIXPACKS_BUILD_CMD = "npm install --only=production"

[build.env]
NODE_ENV = "production"
